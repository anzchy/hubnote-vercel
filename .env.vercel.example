# Vercel 环境变量配置示例
# 复制此文件到 Vercel Dashboard → Settings → Environment Variables

# ===== 必需的环境变量 =====

# Flask 应用密钥（必需）
# 生成命令: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_generated_secret_key_here

# ===== 存储配置（二选一）=====

# 🔥 推荐方案：Vercel KV (Redis 键值存储)
# 优势：速度快、成本低、适合配置和状态数据
# 配置步骤：参考 VERCEL_KV_SETUP.md
STORAGE_TYPE=vercel_kv
# KV_REST_API_URL 和 KV_REST_API_TOKEN 会在创建 KV 数据库后自动添加

# ⚠️ 备选方案：Vercel Blob (对象存储)
# 适用场景：大文件、图片、视频等
# 不推荐用于本项目的配置数据存储
# STORAGE_TYPE=vercel_blob
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxx

# ===== Vercel KV 配置（创建 KV 数据库后自动生成）=====
# 在 Vercel Dashboard → Storage → Create Database → KV
# 创建后这些变量会自动添加，无需手动配置

# KV_REST_API_URL=https://xxxxx.kv.vercel-storage.com
# KV_REST_API_TOKEN=xxxxx

# ===== 可选的环境变量 =====

# Flask 环境
FLASK_ENV=production

# 调试模式（生产环境应设为 False）
FLASK_DEBUG=False

# ===== 注意事项 =====
# 1. ⚠️ Vercel 文件系统是只读的，必须使用 KV 或 Blob 存储数据
# 2. 🔥 强烈推荐使用 Vercel KV（详见 VERCEL_KV_SETUP.md）
# 3. 不要将此文件提交到 Git（已在 .gitignore 中）
# 4. SECRET_KEY 必须保密
# 5. 在 Vercel Dashboard 中逐个添加这些环境变量
# 6. 环境变量修改后需要重新部署才能生效

# ===== 快速开始 =====
# 1. 在 Vercel Dashboard 创建 KV 数据库
# 2. 设置 STORAGE_TYPE=vercel_kv
# 3. 生成并设置 SECRET_KEY
# 4. 重新部署项目
# 详细步骤请参考 VERCEL_KV_SETUP.md
