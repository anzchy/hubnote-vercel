# Vercel 环境变量配置示例
# 复制此文件到 Vercel Dashboard → Settings → Environment Variables

# ===== 必需的环境变量 =====

# Flask 应用密钥（必需）
# 生成命令: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_generated_secret_key_here

# 存储配置（必需）
STORAGE_TYPE=vercel_blob

# Vercel Blob 存储 Token（必需）
# 获取方式: Vercel Dashboard → Storage → Create Blob Store
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxx

# ===== 可选的环境变量 =====

# Flask 环境
FLASK_ENV=production

# 调试模式（生产环境应设为 False）
FLASK_DEBUG=False

# ===== 可选：Vercel KV 存储（如果使用 KV 而不是 Blob）=====

# KV 存储 URL
# KV_REST_API_URL=https://your-kv-store.kv.vercel-storage.com

# KV 存储 Token
# KV_REST_API_TOKEN=your_kv_token_here

# ===== 注意事项 =====
# 1. 不要将此文件提交到 Git（已在 .gitignore 中）
# 2. SECRET_KEY 必须保密
# 3. 在 Vercel Dashboard 中逐个添加这些环境变量
# 4. 环境变量修改后需要重新部署才能生效
